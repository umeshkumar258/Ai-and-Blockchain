{
  "name": "ai-startup-automation-tool",
  "version": "1.0.0",
  "description": "Full-stack AI lead automation dashboard with email and blockchain logging.",
  "scripts": {
    "install-all": "npm.cmd install --prefix backend && npm.cmd install --prefix frontend",
    "dev:backend": "npm.cmd run dev --prefix backend",
    "dev:frontend": "npm.cmd run dev --prefix frontend",
    "server": "npm.cmd run dev --prefix backend",
    "client": "npm.cmd run dev --prefix frontend"
  }
}
